import 'package:flutter/material.dart';

class TelegramFeaturesPage extends StatelessWidget {
  const TelegramFeaturesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
